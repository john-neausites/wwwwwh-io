# Vercel KV Configuration
# These are automatically set by Vercel when you create a KV database
# No need to set manually - Vercel injects them at runtime

KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
